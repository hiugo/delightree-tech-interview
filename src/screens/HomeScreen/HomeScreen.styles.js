import styled from "styled-components/native";

import vars from "@theme/vars";

export const Container = styled.ScrollView`
  flex: 1;
  background-color: #fff;
  padding: ${vars.largeSpacing} ${vars.baseSpacing};
`;
